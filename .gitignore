# 1. 보안 파일 (절대 올리면 안 됨!)
.env

# 2. 가상환경 폴더 (용량이 크고, 서버에서 다시 만들면 됨)
venv/
env/

# 3. 파이썬 캐시 파일 (필요 없음)
__pycache__/
*.pyc

# 4. 로그 파일 (서버 로그 등)
*.log
nohup.out

# 5. VS Code 설정 폴더 (개인 설정이므로 제외)
.vscode/